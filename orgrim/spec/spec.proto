syntax = "proto3";

package spec;

message Metadata {
        map<string,string> tags = 1; 
        map<string,string> properties = 2;
        int64 createdAtNs  = 5;
}

message Envelope {
        Metadata metadata = 1;
        bytes payload = 2;
}
